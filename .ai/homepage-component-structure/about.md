The homepage is organized as a readable composition in `frontend/app/layout.tsx`. The root layout owns the visible section order directly, `HomePageMain` owns the `<main>` shell and floating buy button, and `HomePageEffects` runs client-only page behavior. The older `App`, `HomePage`, and `MarketingSections` indirection has been removed so there is one source of truth for the homepage structure.
