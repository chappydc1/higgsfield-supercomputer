# Task d Plan

1. Update daily prod workflow Playwright command to run only `application-prod-tests.spec.ts`.
2. Verify test selection with `playwright test --list` to confirm expected count.
3. Record review and residual risk for CI-only runtime validation.
