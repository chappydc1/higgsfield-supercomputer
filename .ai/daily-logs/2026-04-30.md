# Daily Log — April 30, 2026

## Completed Tasks

### chore: migrate VM repo path (PR #116)
- Updated deploy config: VM repo path changed from `harrymapodile` → `tobias` folder.

### ci: gate tests to merge-to-main only (PR #117)
- `compile.yml` tests now only run on PRs and pushes to `main`, not on every branch push.
- Prevents hitting GitHub Actions minute cap on non-critical runs.

### fix: seed script missing `/api` prefix (PR #118)
- Seed script default URLs were missing the `/api` prefix, causing 404s on local and CI runs.

### fix: remove borders + fix calendar nav on applicants page
- Stripped `border-zinc-200` from search bar container and date filter pill (+ removed `shadow-sm`).
- Fixed `react-day-picker` v9 calendar navigation arrows: updated classnames from v8 keys
  (`nav_button_previous` / `nav_button_next`) to v9 keys (`button_previous` / `button_next`)
  so month arrows render correctly in the two-month layout.
- Files: `frontend/app/applicants/page.tsx`, `frontend/components/ui/calendar.tsx`

### ci: remove Playwright e2e job from deploy workflow
- Removed the e2e job and `run_playwright` dispatch input from `deploy.yml`.
- E2E testing consolidated into the dedicated `daily-prod-e2e` scheduled workflow.

### ci: remove seed test applicants job from deploy workflow
- Cleaned up `deploy.yml`: removed the seed-test-applicants job that is no longer needed post-deploy.
