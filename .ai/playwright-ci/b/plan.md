# Task b Plan

1. Add an Allure CLI install step in PR E2E workflow after Playwright execution.
2. Add an Allure HTML generation step using the same results directory Playwright writes to.
3. Upload generated `frontend/allure-report` as a workflow artifact for PR visibility.
4. Validate workflow YAML syntax by inspection and targeted diff check.
