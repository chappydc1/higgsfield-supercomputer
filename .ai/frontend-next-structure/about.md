The repository is organized as a two-folder workspace with a Next.js frontend in `frontend/` and a backend placeholder in `backend/`. The frontend keeps standard Next.js app-router files under `frontend/app`, shared UI under `frontend/src`, static assets under `frontend/public`, and page composition split into editable components rather than a single large app file.

