Status: complete
Phases: 3
Assessed: yes

1. Workspace structure
   - [x] Create `frontend/` and `backend/`.
   - [x] Move Next.js files and source directories into `frontend/`.
   - [x] Put static flags under `frontend/public/flags`.

2. Page component split
   - [x] Add `frontend/src/page` components for page shell, page sections, accessibility chrome, cart/modal layers, support widgets, and embeds.
   - [x] Simplify `frontend/src/App.tsx` to compose those components.

3. Validation
   - [x] Run the smallest relevant validation from `frontend/`.
   - [x] Record results and remaining risks.
